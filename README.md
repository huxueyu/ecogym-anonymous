# How to Run?

First config your api key and pricing in `.env` & `configs/model_pricing.yaml`

```bash
python workflow/main.py --benchmark_type {vending, partjob, operation} # partjob is equivalent to freelance in the paper
```

